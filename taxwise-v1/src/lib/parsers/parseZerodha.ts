import { InvestmentTransaction, AssetType } from '../../types/tax';

export const parseZerodha = async (fileData: string): Promise<InvestmentTransaction[]> => {
  // Logic to parse Zerodha P&L statements
  return [];
};
