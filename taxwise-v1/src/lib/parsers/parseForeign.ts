import { InvestmentTransaction, AssetType } from '../../types/tax';

export const parseForeign = async (fileData: string): Promise<InvestmentTransaction[]> => {
  // Logic to parse Foreign Asset statements
  return [];
};
